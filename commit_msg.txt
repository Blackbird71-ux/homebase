feat: opening balance editing + double-entry AP/AR for bills and income

Opening Balances:
- Set OB button on account cards to edit opening balance after creation
- New dialog with amount + date and double-entry warning
- New API endpoint POST /api/finance/accounts/opening-balance

Bills double-entry:
- Invoice Received creates uncleared expense transaction for P&L
- Bill Paid clears the invoice transaction or creates new if none
- Undo Invoice Received deletes the staged expense transaction
- invoiceTxId links bill to its invoice-stage transaction

Income double-entry:
- Remittance Received creates uncleared income transaction for P&L
- Income Received clears the remittance transaction or creates new if none
- Undo Remittance deletes the staged income transaction
- invoiceTxId links income entry to its remittance-stage transaction

Schema and migration:
- Add invoiceTxId to FinanceRecurringBill and FinanceIncomeEntry
- Migration 20260521000000_add_invoice_tx_id
- Add ensureAccountsPayableCategory and ensureAccountsReceivableCategory
